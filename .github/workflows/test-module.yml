name: Module Tests

on:
    pull_request:
    workflow_call:
        inputs:
            module:
                required: true
                type: string
                description: 'LowerCase module name, e.g. billing'
            dependencies:
                required: false
                type: string
                default: ''
                description: 'Space-separated Composer packages, e.g. saucebase/auth'
            extra_patches:
                required: false
                type: string
                default: ''
                description: 'Space-separated optional patches, relative to the module, applied after its own, e.g. patches/optional/bill-workspaces.patch'
            serve_port:
                required: false
                type: string
                default: '8000'
                description: 'Port the E2E server listens on. Use 80 for modules whose URLs are host-based (subdomains), since a host derived from APP_URL drops the port.'

jobs:
    detect-frameworks:
        runs-on: ubuntu-latest
        if: inputs.module != ''
        outputs:
            matrix: ${{ steps.read.outputs.matrix }}
        steps:
            - name: Checkout module
              uses: actions/checkout@v5
              with:
                  path: module-src
            - name: Read supported frameworks from composer.json
              id: read
              run: |
                  fw=$(jq -c '.extra.saucebase.frameworks // ["vue"]' module-src/composer.json 2>/dev/null || echo '["vue"]')
                  echo "matrix=$fw" >> $GITHUB_OUTPUT

    phpunit:
        needs: detect-frameworks
        runs-on: ubuntu-latest
        timeout-minutes: 20
        if: inputs.module != ''
        strategy:
            matrix:
                framework: ${{ fromJSON(needs.detect-frameworks.outputs.matrix) }}
        name: PHPUnit ${{ inputs.module }} (${{ matrix.framework }})

        steps:
            - name: Checkout module
              uses: actions/checkout@v5
              with:
                  path: module-src

            - name: Clone Saucebase
              uses: actions/checkout@v5
              with:
                  repository: saucebase-dev/saucebase
                  ref: main
                  path: saucebase

            - name: Install module from local checkout
              env:
                  MODULE: ${{ inputs.module }}
              run: |
                  pkg=$(echo "$MODULE" | tr '[:upper:]' '[:lower:]')
                  rm -rf saucebase/modules/$pkg
                  cp -r module-src saucebase/modules/$pkg

            - name: Setup PHP
              uses: shivammathur/setup-php@v2
              with:
                  php-version: '8.4'
                  extensions: pdo, pdo_sqlite, mbstring, zip, pcntl, intl, exif
                  coverage: none

            - name: Install dependencies
              if: inputs.dependencies != ''
              working-directory: saucebase
              env:
                  DEPENDENCIES: ${{ inputs.dependencies }}
              run: |
                  for dep in $DEPENDENCIES; do
                      composer require "$dep" --dev --no-interaction --no-scripts
                  done

            - name: Require module in Composer
              working-directory: saucebase
              env:
                  MODULE: ${{ inputs.module }}
              run: |
                  pkg=$(echo "$MODULE" | tr '[:upper:]' '[:lower:]')
                  composer require "saucebase/$pkg:@dev" --no-interaction --no-scripts

            - name: Apply patches
              env:
                  MODULE: ${{ inputs.module }}
                  DEPENDENCIES: ${{ inputs.dependencies }}
                  EXTRA_PATCHES: ${{ inputs.extra_patches }}
              run: |
                  apply_patches() {
                      local dir="$1"
                      for f in "$dir"/*.patch; do
                          [ -f "$f" ] || continue
                          echo "Applying patch: $f"
                          patch -p1 -d saucebase < "$f"
                      done
                  }
                  # Dependencies first (in declared order)
                  for dep in $DEPENDENCIES; do
                      pkg="${dep#saucebase/}"
                      apply_patches "saucebase/modules/$pkg/patches"
                  done
                  # Primary module last
                  mod=$(echo "$MODULE" | tr '[:upper:]' '[:lower:]')
                  apply_patches "saucebase/modules/$mod/patches"
                  # Optional patches the caller opted into, last
                  for f in $EXTRA_PATCHES; do
                      echo "Applying optional patch: $f"
                      patch -p1 -d saucebase < "saucebase/modules/$mod/$f"
                  done

            - name: Install Composer dependencies
              working-directory: saucebase
              run: composer install --prefer-dist --no-interaction --no-progress --no-scripts && composer dump-autoload --no-scripts

            - name: Setup Laravel environment
              uses: ./saucebase/.github/actions/setup-laravel
              with:
                  working-directory: saucebase
                  skip-composer: 'true'
                  skip-php: 'true'
                  framework: ${{ matrix.framework }}

            - name: Run PHPUnit tests
              working-directory: saucebase
              env:
                  MODULE: ${{ inputs.module }}
              run: |
                  pkg=$(echo "$MODULE" | tr '[:upper:]' '[:lower:]')
                  php artisan test modules/$pkg/tests

            - name: Archive saucebase environment
              run: tar --exclude='saucebase/node_modules' -czf saucebase-env.tar.gz saucebase/

            - name: Upload environment artifact
              uses: actions/upload-artifact@v6
              with:
                  name: saucebase-env-${{ inputs.module }}-${{ matrix.framework }}
                  path: saucebase-env.tar.gz
                  retention-days: 1

    e2e:
        runs-on: ubuntu-latest
        timeout-minutes: 30
        needs: [detect-frameworks, phpunit]
        if: inputs.module != ''
        strategy:
            matrix:
                framework: ${{ fromJSON(needs.detect-frameworks.outputs.matrix) }}
        name: E2E ${{ inputs.module }} (${{ matrix.framework }})
        env:
            SERVE_PORT: ${{ inputs.serve_port }}
            # The non-empty branch must sit on `true`: '' is falsy in GitHub
            # expressions, so `cond && '' || format(...)` always falls through.
            APP_URL: http://localhost${{ inputs.serve_port != '80' && format(':{0}', inputs.serve_port) || '' }}

        steps:
            - name: Download environment artifact
              uses: actions/download-artifact@v7
              with:
                  name: saucebase-env-${{ inputs.module }}-${{ matrix.framework }}

            - name: Extract environment
              run: tar -xzf saucebase-env.tar.gz

            - name: Setup PHP
              uses: shivammathur/setup-php@v2
              with:
                  php-version: '8.4'
                  extensions: dom, curl, libxml, mbstring, zip, pcntl, pdo, sqlite, pdo_sqlite
                  coverage: none

            - name: Setup Node.js
              uses: actions/setup-node@v5
              with:
                  node-version: '22'
                  cache: npm
                  cache-dependency-path: saucebase/package-lock.json

            - name: Install npm dependencies
              working-directory: saucebase
              run: npm ci

            - name: Install Playwright
              working-directory: saucebase
              run: npx playwright install chromium --with-deps

            - name: Start Laravel server
              working-directory: saucebase
              run: |
                  # Ports below 1024 are privileged by default. Lower the threshold
                  # rather than serving as root, so the app never writes root-owned
                  # logs, sessions, or SQLite files. Already root (act, containers)
                  # can bind anything and may not have sysctl.
                  if [ "$SERVE_PORT" -lt 1024 ] && [ "$(id -u)" -ne 0 ]; then
                      sudo sysctl -w "net.ipv4.ip_unprivileged_port_start=$SERVE_PORT"
                  fi
                  php artisan serve --host=0.0.0.0 --port="$SERVE_PORT" &

            - name: Wait for server
              run: |
                  timeout 30 bash -c "until curl -f ${APP_URL} > /dev/null 2>&1; do
                    echo 'Waiting for server...'
                    sleep 2
                  done"
                  echo "Server is ready!"

            - name: Run E2E tests
              working-directory: saucebase
              env:
                  MODULE: ${{ inputs.module }}
              run: npx playwright test modules/$MODULE/tests/e2e --max-failures=2

            - name: Upload Playwright report
              if: failure()
              uses: actions/upload-artifact@v6
              with:
                  name: playwright-report-${{ inputs.module }}-${{ matrix.framework }}
                  path: saucebase/playwright-report/
                  retention-days: 7
